{
  "name": "tideline",
  "version": "1.11.2",
  "description": "Files older downloads into folders named for the day they arrived. Native on macOS, Tauri on Windows.",
  "license": "MIT",
  "author": "Elio Struyf",
  "homepage": "https://github.com/estruyf/tideline",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/estruyf/tideline.git"
  },
  "bugs": {
    "url": "https://github.com/estruyf/tideline/issues"
  },
  "scripts": {
    "version": "npm run build",
    "build": "cd app && ./build.sh",
    "build:install": "cd app && ./build.sh --install",
    "build:zip": "cd app && ./build.sh --zip",
    "sign": "cd app && CODESIGN_IDENTITY=\"${CODESIGN_IDENTITY:-$(security find-identity -v -p codesigning | sed -n 's/.*\"\\(Developer ID Application: [^\"]*\\)\".*/\\1/p' | head -1)}\" ./build.sh --zip",
    "notarize": "cd app && CODESIGN_IDENTITY=\"${CODESIGN_IDENTITY:-$(security find-identity -v -p codesigning | sed -n 's/.*\"\\(Developer ID Application: [^\"]*\\)\".*/\\1/p' | head -1)}\" ./build.sh --notarize",
    "release": "cd app && CODESIGN_IDENTITY=\"${CODESIGN_IDENTITY:-$(security find-identity -v -p codesigning | sed -n 's/.*\"\\(Developer ID Application: [^\"]*\\)\".*/\\1/p' | head -1)}\" ./build.sh --notarize",
    "open": "open 'app/dist/Tideline.app'",
    "quit": "osascript -e 'quit app \"Tideline\"' || true",
    "logs": "tail -f \"$HOME/Library/Logs/Tideline.log\"",
    "icon": "cd app && swift Tools/make-icon.swift dist/AppIcon.iconset && open dist/AppIcon.iconset",
    "clean": "rm -rf app/.build app/dist",
    "cask": "./homebrew/publish-cask.sh",
    "site": "./site/stage.sh _site && python3 -m http.server 8080 --directory _site",
    "site:stage": "./site/stage.sh _site",
    "win:dev": "cd windows && npx tauri dev",
    "win:build": "cd windows && npx tauri build",
    "win:test": "cargo test --workspace --manifest-path windows/src-tauri/Cargo.toml",
    "win:check": "cargo check -p tideline-core --manifest-path windows/src-tauri/Cargo.toml --target x86_64-pc-windows-msvc"
  }
}
